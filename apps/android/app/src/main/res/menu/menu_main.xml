<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Le carnet : voir ce qui est enregistre et sous quels reglages. -->

    <item

        android:id="@+id/action_vault"

        android:title="@string/vault_title"

        app:showAsAction="never" />


    <!-- Enregistre les reglages du site affiche, pour ne plus avoir a les
         retenir. -->
    <item
        android:id="@+id/action_save_to_vault"
        android:title="@string/vault_save"
        app:showAsAction="never" />

    <!-- La v1 n'est qu'un secours : retrouver un mot de passe pose avant la v2. -->
    <!-- Visible dans la barre, comme le theme : c'est un mode, pas une option
         enfouie — il decide quel mot de passe sort. -->
    <item
        android:id="@+id/action_algo"
        android:title="@string/algo_mode_v2"
        app:actionViewClass="com.google.android.material.button.MaterialButton"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_dark_mode"
        android:icon="@drawable/ic_dark_mode"
        android:title="@string/menu_dark_mode"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_share"
        android:icon="@drawable/ic_share"
        android:title="@string/menu_share"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_help"
        android:icon="@drawable/ic_help"
        android:title="@string/menu_help"
        app:showAsAction="ifRoom" />

</menu>
